const eventReader = event => {
	const {
		id,
		scheme,
		time,
		data,
		prev
	} = event;



}

const contactEventReader = event => {

	const {
		id,
		time,
		data,
		prev
	} = event;

	if (data) {

	}


}
